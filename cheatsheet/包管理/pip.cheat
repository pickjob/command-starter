; 
; PIP:
;       pip
;           list
;           install
;               --requirement <file>
;               --user
;           freeze > requirements.txt
; 
; 
;           python -m pip completion --zsh >> ~/.zprofile
; 
% pip
@ common

# pip 安装程序
pip install <package_name>