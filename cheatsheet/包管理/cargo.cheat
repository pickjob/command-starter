% cargo
@ common

# Rust更新所有安装程序
cargo install-update -a

; cargo [OPTIONS] [COMMAND]: Rust程序包管理
;       --list: 列出子命令
;       init
;       build
;       run
;       install
;       clean
;
; cargo install cargo-update \
;     zoxide \
;     lsd \
;     fd-find \
;     ripgrep \
;     sd \
;     bat \
;     tre-command \
;     huniq \
;     procs \
;     bottom \
;     hyperfine \
;     git-delta \
;     vivid \
;     navi